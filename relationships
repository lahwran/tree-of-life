relations:
    - parent
    - first child
    - last child
    - next peer
    - last peer

task: no parent, many children, no peers
    task: parent, first child == last child, single following peer
        task: first child and last child; no children or peers
    task: two peers, two children
        task: first child
        task: second child
    task: single previous peer, parent, no children

* parent
+ first child
- last child
= last child is first child
< previous peer
> next peer

+-
    *>+=
        *
    *<>+-
        *>
        *<
    *<


